"""
timeout.py — Per-Node and Global Timeout Demo

This file will demonstrate:
- A node that exceeds its timeout and fails cleanly
- A workflow that exceeds its global timeout and gets cancelled

Implementation coming in Phase 15.
"""
