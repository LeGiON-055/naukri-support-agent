"""
tools.py — Agent Tools

This file will contain the functions that the agent can call:
- check_job_application_status(record_id) — looks up application data
- rag_lookup(question) — searches the knowledge base

Implementation coming in Phase 8.
"""
